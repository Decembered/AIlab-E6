# Video2Motion2Action: Dexterous Manipulation Skill Transfer
# Sub-task 3.3 — Object Shape Reconstruction & IsaacGym Asset
#
# Core dependencies tested with Python 3.8

numpy>=1.21,<2.0
opencv-python>=4.5
torch>=1.13
trimesh>=4.0
scipy>=1.7
matplotlib>=3.5
h5py>=3.0
segment-anything>=1.0

# Optional: IsaacGym (Preview 4) — for asset validation only
# isaacgym is installed separately via ~/opt/isaac_gym/
