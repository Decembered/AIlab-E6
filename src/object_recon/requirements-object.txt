# ============================================================================
# Object Reconstruction Dependencies — Member C
# Track T-1: Video2Motion2Action
# ============================================================================

# ============================================================================
# TIER 0 — Core mesh & array processing (always install)
# ============================================================================
numpy>=1.24,<2.0
trimesh>=4.0
open3d>=0.18
scipy>=1.10

# ============================================================================
# TIER 1 — 3D reconstruction essentials
# ============================================================================
# For 3DGS: install gsplat or nerfstudio separately (heavy CUDA deps)
# For image-to-3D: InstantMesh / TripoSR (see their repos)
# For COLMAP: install colmap via system package manager
torch>=2.0
torchvision>=0.15
matplotlib>=3.7
opencv-python>=4.8
pillow>=10.0
tqdm>=4.65
rich>=13.0
pyyaml>=6.0

# ============================================================================
# TIER 2 — SAM / segmentation
# ============================================================================
# segment-anything>=1.0       # SAM1
# sam2>=1.0                   # SAM2 — may need separate install
# groundingdino>=0.1          # For text-prompted detection

# ============================================================================
# TIER 3 — Mesh post-processing
# ============================================================================
# vhacd>=0.1                  # Convex decomposition (for collision meshes)
# pymeshlab>=2023.12          # Advanced mesh processing (optional)

# ============================================================================
# TIER 4 — IsaacGym (on cluster only)
# ============================================================================
# IsaacGym is pre-installed on the cluster at ~/opt/isaac_gym/isaacgym
# For local testing: isaacgym (Preview 4) requires manual download from NVIDIA
